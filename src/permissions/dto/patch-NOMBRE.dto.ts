import { Type} from "class-transformer";
import { IsString, IsEmail, IsDate, IsNumber } from "class-validator"; //Eliminar importaciones que no se utilicen

export class UpdateNombreDto{ //Cambiar nombre por lo que corresponda
}

